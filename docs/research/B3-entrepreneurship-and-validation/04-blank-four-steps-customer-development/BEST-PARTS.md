# BEST-PARTS — Blank, The Four Steps to the Epiphany

## ADOPT
1. **Map "search vs. execution" onto AutoFirm's gate model.** Customer Discovery + Validation =
   AutoFirm's L2.B3 *validation phase* (search for a repeatable model); Customer Creation + Company
   Building = the build/market/operate phases (execution). AutoFirm must NOT enter the execution
   gates until the validation gate is green — directly enforces CLAUDE SS4.2 gate-based phases.
2. **Customer Discovery first — "build a product nobody wants" is the #1 risk.** AutoFirm's company
   pipeline begins with an automated discovery step (problem existence + importance) BEFORE any build
   capability (B13/B14) is engaged. This is the entrepreneurial analog of "back-end/data first."
3. **"Earlyvangelist"/visionary-customer validation as the SOM proxy.** Validation against a narrow
   beachhead segment maps to the SOM (serviceable obtainable market) concept (source 10) — the first
   capturable slice, proven before scaling.

## REJECT / DEFER
- **Reject as standalone effectiveness evidence** (Low tier, no internal empirics) — cite RCTs/SLR.
- **Defer the org-transition (step 4) detail** to B1 (operating models) and L2.ORG — note the
  search->execution handoff here, design the org mechanics there.

## Build implication
The four steps become AutoFirm's **company-lifecycle state machine**: DISCOVER -> VALIDATE -> CREATE
-> BUILD-COMPANY, with a hard gate after VALIDATE. The state is externalized data (CLAUDE SS A3 resume),
making the pipeline auditable and resumable.
